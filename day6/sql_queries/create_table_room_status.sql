DROP TABLE IF EXISTS room_status;

-- CREATE TABLE IF NOT EXISTS room_status (
-- 	uid INT PRIMARY KEY AUTO_INCREMENT,
--     occupied_sdate DATE ,
--     occupied_edate DATE,
--     CHECK (occupied_sdate <= occupied_edate),
--     room_id INT,
--     CONSTRAINT FK_rs_rd FOREIGN KEY (room_id) REFERENCES room_detail(room_id)  
-- 	);
